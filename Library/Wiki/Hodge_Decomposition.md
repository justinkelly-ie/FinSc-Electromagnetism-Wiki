# 🧩 Electromagnetic Field Hodge Decomposition

> [[Electromagnetism](Electromagnetism.md)] — Row 10 Discrete Exterior Calculus

This module documents the **Discrete Helmholtz-Hodge Decomposition Theorem** for electromagnetic field state vectors (`Vexel`).

---

## 1. Literate Idris 2 Implementation

```idris
module Wiki.Hodge_Decomposition

import QuickCheck
import Math.Multiset
import Math.BoxInt
import Math.Pixel
import Core.BoxInt
import Core.VexelMaxel
import EM.Potential
import EM.Hodge
import EM.Calculus

%default total
```

---

## 2. Mathematical Principle

Any arbitrary electromagnetic state vector field $V$ mapped over a causal substrate grid decomposes into three mutually orthogonal physical components:

$$V = V_{\text{harmonic}} \oplus \nabla \Phi \oplus (\nabla \times \mathbf{A})$$

1. **Harmonic Field ($V_{\text{harmonic}}$)**: Vacuum radiation modes satisfying $\Delta V = 0$.
2. **Gradient Field ($\nabla \Phi$)**: Pure irrotational electrostatic fields generated by scalar charge potentials.
3. **Curl Field ($\nabla \times \mathbf{A}$)**: Solenoidal magnetostatic fields generated by closed current loops.

---

## 3. Executable Verification Properties

```idris
||| Vacuum Field Orthogonality: Vacuum vector potential produces zero magnetic field.
public export
prop_hodgeVacuumOrthogonality : Property
prop_hodgeVacuumOrthogonality = property (
  let eField = vacuumVectorPotential
      faces = []
      bField = computeMagneticField faces eField
  in bField == MkMaxel [])
```
